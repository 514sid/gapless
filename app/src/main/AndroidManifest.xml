<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.GaplessSample"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="userLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PlayerActivity"
            android:process=":player"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="userLandscape" />

        <!-- Keepalive stub in :player process — WatchdogService holds a binding to this.
             When :player crashes this service dies and onServiceDisconnected fires. -->
        <service
            android:name=".PlayerService"
            android:process=":player"
            android:exported="false" />

        <!-- Foreground watchdog in the main process — survives :player crashes. -->
        <service
            android:name=".WatchdogService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

    </application>

</manifest>
